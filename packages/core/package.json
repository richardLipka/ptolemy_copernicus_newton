{
  "name": "@orrery/core",
  "version": "0.4.0",
  "type": "module",
  "description": "Solar-system model data and the four historical engines — Ptolemaic, Copernican, Keplerian and Newtonian — as a dependency. No DOM, no framework, no runtime dependencies.",
  "license": "MIT",
  "author": "Richard Lipka",
  "repository": {
    "type": "git",
    "url": "https://github.com/richardLipka/ptolemy_copernicus_newton.git",
    "directory": "packages/core"
  },
  "engines": {
    "node": ">=20"
  },
  "//": "Source is exported directly: both consumers are Vite apps, which transform TypeScript in linked workspace dependencies. Publishing to a registry would need a build step emitting .js and .d.ts.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts",
    "./package.json": "./package.json"
  },
  "sideEffects": false,
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:vsop87": "node scripts/generate-vsop87.mjs"
  },
  "devDependencies": {
    "astronomia": "^4.2.0"
  }
}
